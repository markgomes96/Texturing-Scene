#ifndef MENU_H
#define MENU_U

#include "structs.h"

extern enum State activeState;
//extern const int WINDOW_MAX_X, WINDOW_MAX_Y;

void startDisplay();
void pauseDisplay();
void overDisplay();
void instructDisplay();
//enum State activeState = gameState;
#endif
