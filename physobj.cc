#ifndef PHYSOBJ_CC
#define PHYSOBJ_CC

#include "includes.h"
#include "physobj.h"

/*
* Makes an object have physical properties
*/

PhysObj::PhysObj() 
{ }

void PhysObj::createBounds(box *face, vect3 wpos)
{
	//Make box around vertices and move it to world space using position
}

#endif
